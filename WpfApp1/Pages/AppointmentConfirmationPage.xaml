<Page x:Class="WpfApp1.Pages.AppointmentConfirmationPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:WpfApp1.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="AppointmentConfirmationPage">

    <Grid Background="FloralWhite">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*"></ColumnDefinition>
            <ColumnDefinition Width="2*"></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="30"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2" FontSize="24" FontWeight="Bold" VerticalAlignment="Top" Foreground="White" Background="MediumPurple" TextAlignment="Center" Text="{Binding Services.Name}"></TextBlock>
        <StackPanel x:Name="MoviePanel" Grid.Column="0" Grid.Row="1" Grid.RowSpan="2" VerticalAlignment="Top">
            <Image Source="{Binding Services.Users.ImagePath}" Width="Auto"/>
            <TextBlock FontSize="18" FontWeight="Bold" Text="{Binding Services.Users.Name}" HorizontalAlignment="Center"/>
            <TextBlock Text="{Binding Services.Users.Phone, StringFormat=Phone: {0}}" HorizontalAlignment="Center"/>
            <TextBlock Text="{Binding Services.Users.Email, StringFormat=Email: {0}}" HorizontalAlignment="Center"/>
            <Button Width="100" BorderBrush="White" BorderThickness="2" Background="DeepPink" Foreground="White" Click="MasterButton_Click">Master Page</Button>
        </StackPanel>
        <StackPanel Grid.Column="1" Grid.Row="1" VerticalAlignment="Top" HorizontalAlignment="Stretch">
            <TextBlock Text="Service Info" FontWeight="Bold" FontSize="20" HorizontalAlignment="Center"/>
            <StackPanel>
                <TextBlock Text="{Binding Services.Name}" FontWeight="Bold" HorizontalAlignment="Center"/>
                <TextBlock Text="{Binding Services.Description}" HorizontalAlignment="Center"/>
                <TextBlock Text="{Binding Services.Price, StringFormat={}{0} Rub.}" HorizontalAlignment="Center"/>
                <TextBlock Text="{Binding Date, StringFormat={}{0:d MMMM}}" HorizontalAlignment="Center" FontWeight="Bold"/>
                <TextBlock Text="{Binding Date, StringFormat={}{0:dd.MM.yyyy}}" HorizontalAlignment="Center" FontSize="8" Foreground="Gray"/>
                <TextBlock Text="{Binding Date, StringFormat={}{0:HH:mm}}" Margin="10" HorizontalAlignment="Center" FontSize="22"/>
            </StackPanel>
            <Button Height="auto" Width="auto" Background="MediumPurple" Foreground="White" Margin="40 0" Click="Button_Click" FontSize="30" FontWeight="Bold">Book</Button>
        </StackPanel>
    </Grid>
</Page>
