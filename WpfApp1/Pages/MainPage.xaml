<Page x:Class="WpfApp1.Pages.MainPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:WpfApp1.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="MainPage">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*"/>
            <ColumnDefinition Width="4*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="40"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal" Grid.Row="0" Grid.Column="1">
            <TextBlock VerticalAlignment="Center" Margin="5">Search: </TextBlock>
            <TextBox x:Name="SearchBar" Width="150" Height="20" TextChanged="SearchBar_TextChanged"></TextBox>
            <TextBlock VerticalAlignment="Center" Margin="5">Manufacturer:</TextBlock>
            <ComboBox Width="100" Height="20" x:Name="SortBox" VerticalAlignment="Center" SelectionChanged="SortBox_SelectionChanged" SelectedIndex="0"></ComboBox>
            <TextBlock VerticalAlignment="Center" Margin="5">Part:</TextBlock>
            <ComboBox Width="100" Height="20" x:Name="PartTypeBox" VerticalAlignment="Center" SelectedIndex="0" SelectionChanged="PartTypeBox_SelectionChanged"></ComboBox>
        </StackPanel>
        <ListBox x:Name="ProductListBox" ScrollViewer.HorizontalScrollBarVisibility="Disabled" Background="FloralWhite" Grid.Column="1" Grid.Row="1">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel Height="260" Width="220">
                        <Image Height="120" Width="120" Source="{Binding image}"/>
                        <TextBlock FontSize="18" FontWeight="Bold" Text="{Binding name}" HorizontalAlignment="Center" TextAlignment="Center" TextWrapping="Wrap"/>
                        <TextBlock Text="{Binding price, StringFormat=Price: {0}}" HorizontalAlignment="Center"/>
                        <TextBlock Text="{Binding manufacturer_.name, StringFormat=Manufacturer: {0}}" HorizontalAlignment="Center"/>
                        <TextBlock Text="{Binding parttype_.name, StringFormat=Type: {0}}" HorizontalAlignment="Center"/>
                        <Button Margin="5" Width="120" Height="20">Add</Button>
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
            <ListBox.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel/>
                </ItemsPanelTemplate>
            </ListBox.ItemsPanel>
        </ListBox>
    </Grid>
</Page>
