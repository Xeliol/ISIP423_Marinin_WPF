<Page x:Class="WpfApp1.Pages.MainPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:WpfApp1.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Page1">

    <Grid>
        <ListBox x:Name="UserListBox" ScrollViewer.HorizontalScrollBarVisibility="Disabled">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel Height="200" Width="150">
                        <Image Height="120" Width="120" Source="{Binding ImgPath}"/>
                        <TextBlock Margin="5" Text="{Binding Name, StringFormat=Name: {0}}" HorizontalAlignment="Center"/>
                        <TextBlock Margin="5" Text="{Binding Price, StringFormat=Price: {0}}" HorizontalAlignment="Center"/>
                        <Button Width="120" Height="20" Click="Button_Click">Add to Cart</Button>
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
            <ListBox.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel/>
                </ItemsPanelTemplate>
            </ListBox.ItemsPanel>
        </ListBox>
        <Button x:Name="NextBut" Height="50" Width="100" Margin="20" HorizontalAlignment="Right" VerticalAlignment="Bottom" Click="NextBut_Click">Next</Button>
    </Grid>
</Page>
