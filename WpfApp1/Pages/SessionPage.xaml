<Page x:Class="WpfApp1.Pages.SessionPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:WpfApp1.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="SessionPage">

    <Grid Background="FloralWhite">
        <TextBlock Height="30" VerticalAlignment="Top" Background="MediumPurple"></TextBlock>
        <TextBlock FontSize="24" HorizontalAlignment="Center" FontWeight="Bold" Foreground="White">Session Details</TextBlock>
        <StackPanel Margin="0 50">
            <ListBox x:Name="SeatListBox" Width="650" Height="300" ScrollViewer.HorizontalScrollBarVisibility="Disabled" HorizontalAlignment="Center" Background="FloralWhite">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Height="50" Width="50" HorizontalAlignment="Center">
                            <Button Width="50" Height="50" Click="Button_Click" Background="Gray" Foreground="Black"></Button>
                        </StackPanel>
                    </DataTemplate>
                </ListBox.ItemTemplate>
                <ListBox.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel/>
                    </ItemsPanelTemplate>
                </ListBox.ItemsPanel>
            </ListBox>
            <TextBlock HorizontalAlignment="Center">Select your seats</TextBlock>
            <Button x:Name="ConfirmButton"  Width="100" BorderBrush="White" BorderThickness="2" Background="MediumPurple" Foreground="White" Click="ConfirmButton_Click">Confirm</Button>
        </StackPanel>
    </Grid>
</Page>
