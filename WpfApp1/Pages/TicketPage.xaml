<Page x:Class="WpfApp1.Pages.TicketPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:WpfApp1.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="TicketPage">

    <Grid Background="FloralWhite">
        <TextBlock Height="30" VerticalAlignment="Top" Background="MediumPurple"></TextBlock>
        <TextBlock FontSize="24" HorizontalAlignment="Center" FontWeight="Bold" Foreground="White">Buying Ticket</TextBlock>
        <StackPanel Margin="0 40" Width="400" VerticalAlignment="Top" HorizontalAlignment="Center">
            <TextBlock Text="Seats" FontWeight="Bold" FontSize="24" HorizontalAlignment="Center"/>
            <TextBlock Text="{Binding TheaterID, StringFormat=Theater №{0}}" HorizontalAlignment="Center"/>
            <TextBlock Text="{Binding Theaters.Rating, StringFormat=Rating: {0}}" HorizontalAlignment="Center"/>
            <TextBlock Text="{Binding Time, StringFormat={}{0:dd MMMM, hh:mm tt}}" HorizontalAlignment="Center"/>
            <ListBox x:Name="SeatsListBox" ScrollViewer.HorizontalScrollBarVisibility="Disabled" Margin="0 10 0 0" Background="FloralWhite">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Height="20" Width="400">
                            <TextBlock Text="{Binding}" HorizontalAlignment="Center"/>
                        </StackPanel>
                    </DataTemplate>
                </ListBox.ItemTemplate>
                <ListBox.ItemsPanel>
                    <ItemsPanelTemplate>
                        <StackPanel/>
                    </ItemsPanelTemplate>
                </ListBox.ItemsPanel>
            </ListBox>
            <TextBlock x:Name="PriceText" Text="Price: " FontWeight="Bold" FontSize="24" HorizontalAlignment="Center"/>
            <Button x:Name="BuyButton" Margin="0 10" Height="40" Width="100" FontSize="20" FontWeight="Bold" BorderBrush="White" BorderThickness="2" Background="MediumPurple" Foreground="White" Click="ConfirmButton_Click">Purchase</Button>
        </StackPanel>
    </Grid>
</Page>
