<Page x:Class="WpfApp1.Pages.MoviePage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:WpfApp1.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="MoviePage">

    <Grid Background="FloralWhite">
        <TextBlock Height="30" VerticalAlignment="Top" Background="MediumPurple"></TextBlock>
        <StackPanel Margin="0 30" Orientation="Horizontal">
            <StackPanel x:Name="MoviePanel" Height="400" Width="360" HorizontalAlignment="Left">
                <Image Margin="20" Height="240" Width="240" Source="{Binding PosterPath}"/>
                <TextBlock FontSize="18" FontWeight="Bold" Text="{Binding Name}" HorizontalAlignment="Center"/>
                <TextBlock Text="{Binding Genre, StringFormat=Genre: {0}}" HorizontalAlignment="Center"/>
                <TextBlock Text="{Binding Rating, StringFormat=Rating: {0}}" HorizontalAlignment="Center"/>
                <TextBlock Text="{Binding Rating, StringFormat=Age Rating: {0}}" HorizontalAlignment="Center"/>
                <TextBlock Text="{Binding ReleaseDate, StringFormat={}{0:dd-MMM-yyyy}}" HorizontalAlignment="Center"/>
            </StackPanel>
            <StackPanel Width="400">
                <TextBlock Text="{Binding Name}" FontWeight="Bold" FontSize="24" HorizontalAlignment="Center"/>
                <TextBlock Text="{Binding Description}" FontSize="14" HorizontalAlignment="Center" TextWrapping="Wrap"/>
                <Button Margin="5" Width="240" Height="50" Click="Button_Click" FontSize="30" FontWeight="Bold" Background="MediumPurple" Foreground="White">Buy a Ticket</Button>
            </StackPanel>
        </StackPanel>
        <TextBlock FontSize="24" HorizontalAlignment="Center" FontWeight="Bold" Foreground="White">Movie Details</TextBlock>
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Top">
            <Button HorizontalAlignment="Right" VerticalAlignment="Top" Margin="5" Width="100" Height="20" Click="AccountButton_Click" BorderBrush="White" BorderThickness="2" Background="MediumPurple" Foreground="White">My Account</Button>
        </StackPanel>
    </Grid>
</Page>
