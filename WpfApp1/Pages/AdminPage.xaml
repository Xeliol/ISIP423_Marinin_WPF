<Page x:Class="WpfApp1.Pages.AdminPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:WpfApp1.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="AdminPage">

    <Grid Background="FloralWhite">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*"></ColumnDefinition>
            <ColumnDefinition Width="3*"></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="30"></RowDefinition>
            <RowDefinition Height="4*"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="3" FontSize="24" FontWeight="Bold" VerticalAlignment="Top" Foreground="White" Background="MediumPurple" TextAlignment="Center">Account</TextBlock>
        <StackPanel Grid.Column="0" Grid.Row="1" VerticalAlignment="Top">
            <Button x:Name="LogOutButton"  Width="100" BorderBrush="White" BorderThickness="2" Margin="0 50" Background="MediumPurple" Foreground="White" Click="LogOutButton_Click">Log Out</Button>
        </StackPanel>
        <StackPanel Grid.Column="1" Grid.Row="1" Margin="10">
            <TextBlock Text="Users" FontWeight="Bold" FontSize="24" HorizontalAlignment="Center"/>
            <ListBox x:Name="UserListBox" ScrollViewer.VerticalScrollBarVisibility="Visible" Height="300" ScrollViewer.CanContentScroll="True" Margin="0 30 0 0" Background="FloralWhite">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <StackPanel>
                            <Label>Login:</Label>
                            <TextBox Text="{Binding Login}" Width="150" Height="20" HorizontalAlignment="Left"/>
                            <Label>Name:</Label>
                            <TextBox Text="{Binding Name}"  Width="150" Height="20" HorizontalAlignment="Left"/>
                            <Label>Email:</Label>
                            <TextBox Text="{Binding Email}" Width="150" Height="20" HorizontalAlignment="Left"/>
                            <Label>Phone:</Label>
                            <TextBox Text="{Binding Phone}" Width="150" Height="20" HorizontalAlignment="Left"/>
                            <Label>User Type:</Label>
                            <TextBox Text="{Binding TypeID}" Width="150" Height="20" HorizontalAlignment="Left"/>
                            <Button Height="20" Width="150" Margin="0 10" Background="Red" Foreground="White" HorizontalAlignment="Left" Click="RemoveButton">Remove</Button>
                        </StackPanel>
                    </DataTemplate>
                </ListBox.ItemTemplate>
                <ListBox.ItemsPanel>
                    <ItemsPanelTemplate>
                        <StackPanel/>
                    </ItemsPanelTemplate>
                </ListBox.ItemsPanel>
            </ListBox>
        </StackPanel>
        <StackPanel Grid.Row="2" Grid.Column="1">
            <Button Click="AddButton" Height="20" Width="100" Margin="10" Background="MediumPurple" Foreground="White">Add User</Button>
            <Button Height="20" Width="100" Margin="10" Background="MediumPurple" Foreground="White" Click="EditButton">Save Changes</Button>
        </StackPanel>
    </Grid>
</Page>
